# Phase 9 面向用户与题目二 A–E 验收矩阵

> **历史架构记录：** 本文描述 2026-08 的 Phase 9 多模态基线；当前运行架构已移除 Qwen3-VL / 8890，不可作为当前启动、部署或验收说明。

> 日期：2026-08-12
> 判定原则：只记录已验证事实；CPU、mock 和自动浏览器通过不代替真实 GPU 或用户在当前电脑的正式验收。公网和跨电脑部署已取消。

## 状态

| 状态 | 含义 |
| --- | --- |
| 已满足 | 当前代码和可重复测试已通过。 |
| 部分满足 | 实现已完成，但仍缺真实 GPU 或用户本机验收。 |
| 待 GPU | 必须等待 detector/Qwen3-VL 与服务器 GPU。 |
| 待人工 | 需要用户在当前电脑操作或确认。 |
| 不适用 | 已取消的公网或跨设备交付项，仅保留兼容代码测试。 |

## A–E 满分条件映射

| 评分项 | 用户操作 | 页面/接口/数据 | 满分可观察结果 | 当前证据与实际结果 | 状态 | 后续门 |
| --- | --- | --- | --- | --- | --- | --- |
| A 上传与拍照 | 当前电脑选图；手机尺寸响应式检查 | 首页；`POST /api/cases` | JPG/PNG/WebP 可上传，填写作物、部位、生育期、环境 | 首页支持文件选择、预览与 15MB 提示；接口测试通过 | 已满足 | 当前电脑 Chrome/Edge 复测 |
| A 田间信息 | 填受害比例与扩散速度 | `affected_ratio_percent`、`spread_speed` | 0–100 校验并持久化；缺失时严重度不猜测 | 迁移、保存和“无法判断”规则测试通过 | 已满足 | 真实两阶段分析复测 |
| A 完整闭环 | 上传→检测→分析→历史→报告 | 首页、详情、历史、报告 | 一条清晰用户路径；失败后病例仍保留 | 新病例完成真实上传、检测、两阶段分析、历史、趋势、报告和复核 | 已满足（自动） | 用户 Chrome/Edge 复测 |
| B 16 类检测 | 上传 16 类真实样本 | detector 8870；检测框层 | 每类显示位置、类别、置信度 | Phase 9 每类 1 张真实冒烟 16/16 成功；正式评估每类 10 张 | 已满足 | 用户抽查检测框 |
| B 多目标/无目标/低置信 | 上传对应样本 | `detector_summary`、自动结果提示 | 每种路径行为明确且不伪造确诊 | 自动状态测试通过；真实无目标病例显示“无法判断、请补拍”；固定 160 张覆盖低置信与多目标证据 | 已满足（自动） | 用户按提示补拍复测 |
| B 专家证据 | 上传类 10/13 候选 | 技术证据折叠区 | 两个 crop 专家和一个检测专家只给第二意见 | GPU health 确认三个视觉模型加载，`routing.mode=shadow` | 已满足 | 用户展开技术证据抽查 |
| C 独立图像判断 | 提交检测后的病例 | `phase9-multimodal-v2` 阶段一 | Qwen3-VL 不接收 YOLO 类别，避免结论锚定 | prompt 与两阶段 mock 已验证 | 已满足（协议） | 正式 160 张真实评估 |
| C 证据比对 | 等待第二阶段 | 阶段二请求/结构化分析 | 比较独立判断、YOLO、shadow 专家和质量风险 | Schema、冲突门、风险只升不降测试通过 | 已满足（协议） | 冲突 17 样本识别率 ≥80% |
| C 内容完整 | 查看综合分析 | 诊断、症状、危害、原因、证据、不确定性、补拍信息 | 成功/Schema/关键内容完整率均 100% | 恢复后 160/160 成功；Schema/内容 100%；Top-1 87.50%；E2E P95 5.105 秒 | 已满足 | 用户阅读真实病例 |
| C 安全门 | 查看风险和复核 | `diagnostic_risk`、`needs_human_review` | 既有风险错误清除为 0；不安全农药输出为 0 | 正式 160 张不安全输出 0、风险错误清除 0；冲突识别 15/17 | 已满足 | 用户抽查冲突/复核提示 |
| D 逐类知识 | 打开结果/报告来源 | `phase9-knowledge-v2` | 16/16 类至少一个逐类权威来源 | 16 类来源、标题、机构、URL、日期、范围测试通过 | 已满足 | 现场抽查链接可访问性 |
| D 分层防治 | 查看下一步 | 农业、物理、生物、监测升级 | 16/16 分类完整或明确不适用 | 知识契约测试通过 | 已满足 | 人工内容审校 |
| D 用药安全 | 查看化学防治边界 | 安全声明 | 无产品、剂量、混配、次数、安全间隔 | 只提示当地登记标签与植保人员 | 已满足 | 正式 160 张输出扫描 |
| E 严重度 | 填田间信息后分析 | `field_severity`、`severity_basis` | 结合比例与扩散速度；矛盾转复核 | 正式 160 张引用率 100%；本机 E2E 显示 12.5%/缓慢→较低并保留依据 | 已满足（自动） | 用户填写不同输入复测 |
| E 病例历史 | 上传并刷新 | `/history`、`GET /api/cases` | 默认显示 SQLite 全部病例，不要求公开同意，不自动过期 | 本地接口测试通过；旧病例可见 | 已满足（自动） | 当前电脑刷新与重启复测 |
| E 趋势 | 切换 7/30 天 | `/trends`、`GET /api/trends` | 统计本机全部记录；数量、作物、候选、严重度和复核状态可追溯 | 页面、接口和免责声明通过 | 已满足（自动） | 与正式 SQLite 抽样核对 |
| E 报告 | 打开病例并打印 | `/reports/{id}`、report API | 原图、框、田间信息、双模型证据、风险、来源、编号可打印/PDF | 浏览器检查通过；旧记录缺字段显示“历史记录未提供” | 已满足（本地） | 当前电脑 Edge PDF 保存 |
| 本机长期保存 | 查看带旧过期时间的病例 | SQLite、历史、趋势 | 本机启动与查询不删除病例或图片 | 后端测试验证已过期记录仍可见且图片存在 | 已满足（自动） | 前后端重启实测 |
| 公网隐私/修改/限流 | 不进入默认本机流程 | `CROP_PUBLIC_MODE=true` 兼容代码 | 默认关闭，不影响本机病例 | 原 422/403/429/过期清理回归继续通过 | 不适用 | 无公网部署任务 |
| Sites 管理隔离 | 不发布 Sites | Worker 兼容代码 | 默认关闭，不进入交付 | Worker render tests 保留 | 不适用 | 无公网部署任务 |
| 数据持久化 | 重启、备份、恢复 | SQLite WAL、维护脚本 | 完整性 ok，备份恢复后图片路径可核验 | 在线备份/恢复与 FastAPI 重启实测通过；完整性 ok、23/23 图片有效；普通历史排除测试记录 | 已满足（自动） | 用户刷新/重启复测 |

## 本轮自动、GPU 与浏览器证据

- 后端：`backend\.venv\Scripts\python.exe -m pytest -q` → 24 passed，1 个 Starlette 弃用警告。
- 前端：`npm.cmd test` → Vinext build 通过，6/6 render/Worker tests 通过。
- Lint：0 errors，5 个动态病例图片的 `no-img-element` 性能提示。
- 数据库：实施前备份已创建；21 条既有记录与 2 条本轮真实自动回归记录均标记为测试，普通历史/趋势为 0、`/admin` 可追溯；当前库完整性 `ok`、23/23 图片路径有效。
- GPU：`artifacts/server/phase9-post-remediation-multimodal-evidence-20260812.json` 为服务恢复后的固定 16 类×10 张复测；160/160 成功、Top-1 87.50%、Schema/内容/严重度引用 100%、冲突 15/17、不安全与风险保护失败 0、E2E P95 5.105 秒、峰值显存 23,088 MiB。
- 六模型：YOLO26n/26s/11s/12s/v8s/v5su 6/6 完成公平对照；YOLO12s 全量重训后冻结 mAP50-95 `0.523463` 未超过现网 `0.548224`，按硬门拒绝晋级，未利用冻结集继续调参。
- 浏览器：真实病例详情、趋势和报告均完成 DOM/视觉检查，证据为 `phase9-local-e2e-detail-20260812.png`、`phase9-local-trends-20260812.png`、`phase9-local-report-20260812.png`。
- 故障恢复：双隧道关闭时新病例保存且状态为 `model_unavailable`，历史/报告可读；仅 VLM 不可用时检测框保留，恢复后只重试分析成功。
- 本地服务：前端 3000、FastAPI 8000、detector 8870、Qwen3-VL 8890 当前均可用，detector 路由保持 `shadow`。

## 尚不能宣称满分的硬门

1. 用户在当前电脑的 Chrome/Edge 亲自完成上传、分析、历史、趋势、PDF、失败重试、刷新和前后端重启恢复测试。
2. 用户确认关键用例可行并反馈最终视觉偏好后，才进入 9.9 最终 UI/UX 重构。
